{
  "name": "cap-odata-transformer",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist coverage",
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "check:format": "prettier --check \"src/**/*.ts\" \"tests/**/*.ts\"",
    "link:global": "npm link",
    "link:local": "npm link cap-odata-transformer",
    "unlink:local": "npm unlink cap-odata-transformer && npm install",
    "release:patch": "npm version patch && npm publish --access public",
    "release:minor": "npm version minor && npm publish --access public"
  },
  "keywords": [
    "sap",
    "cap",
    "odata",
    "cqn",
    "successfactors",
    "query",
    "transform"
  ],
  "author": "Samad & Soultan Hatsijev",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.6.2",
    "jest": "^30.2.0",
    "prettier": "^3.6.2",
    "ts-jest": "^29.4.4",
    "tsup": "^8.5.0",
    "typescript": "^5.9.3"
  }
}
